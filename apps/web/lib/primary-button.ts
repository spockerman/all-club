/**
 * Botão de ação principal — aparência forte e neutra (padrão do projeto).
 * Use em `<Link>` ou `<button>` para CTAs primários.
 */
export const primaryButtonClassName =
  'inline-flex items-center justify-center gap-2 bg-gray-900 text-white px-4 py-2 rounded-lg text-xs font-bold hover:bg-gray-800 transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-gray-400 focus-visible:ring-offset-2 disabled:opacity-50 disabled:pointer-events-none'

/**
 * Botão secundário — contorno neutro, mesma família visual do primário.
 */
export const outlineButtonClassName =
  'inline-flex items-center justify-center gap-1 border border-gray-200 bg-white text-gray-900 px-3 py-1.5 rounded-lg text-[11px] font-bold hover:bg-gray-50 transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-gray-400 focus-visible:ring-offset-1'

/**
 * Links de ação em tabelas / texto — coerente com botões (neutro escuro).
 */
export const linkActionClassName = 'text-gray-900 font-medium hover:underline'
